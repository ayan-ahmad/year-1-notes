## Initialise
`git init`
## Status
`git status`
## Clone
`git clone <url>`
## Commit
`git add .`
`git status`
`git commit -m "Commit Message"`
## Push
`git push`
or
`git push <remote> <branch>`
## Branches
`git checkout <branch>`
## Rollback
`git reset HEAD~`